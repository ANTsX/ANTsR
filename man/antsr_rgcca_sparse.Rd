% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multiscaleSVDxpts.R
\name{antsr_rgcca_sparse}
\alias{antsr_rgcca_sparse}
\title{Sparse Multi-view CCA Baseline using RGCCA (version-safe)}
\usage{
antsr_rgcca_sparse(
  data_matrices,
  ncomp = 3,
  tau = 0.5,
  sparsity = 0.3,
  scheme = "factorial",
  scale = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{data_matrices}{Named list of subject × feature matrices. Each must have the same number of rows.}

\item{ncomp}{Number of canonical components per view (default = 3)}

\item{tau}{Numeric in [0,1] or vector. Regularization per view (0 = pure CCA, 1 = PCA limit)}

\item{sparsity}{Numeric in (0,1]. L1 sparsity per view; lower = sparser (default = 0.3)}

\item{scheme}{RGCCA optimization scheme: 'factorial', 'centroid', or 'horst' (default = 'factorial')}

\item{scale}{Logical. Whether to z-score features (default = TRUE)}

\item{verbose}{Logical. Print progress messages (default = FALSE)}
}
\value{
A list with:
  \item{loadings}{List of sparse feature loadings (features × components)}
  \item{scores}{List of subject × component canonical variates}
  \item{shared_embedding}{Average canonical embedding across modalities}
  \item{correlations}{Canonical correlations across shared space}
}
\description{
Implements a sparse multi-view canonical correlation analysis using RGCCA,
enforcing sparsity on each modality to obtain interpretable feature loadings.
Compatible with RGCCA >= 3.0 (using `blocks` and `connection` arguments).
}
