% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fusedRidge.R
\name{fusedRidgeDirect}
\alias{fusedRidgeDirect}
\title{Direct Manifold-Regularized Fused Ridge Regression (General)}
\usage{
fusedRidgeDirect(
  X_pcs,
  y_raw,
  thresholds,
  covariates = NULL,
  lambda1 = 0.5,
  lambda2 = 0.5,
  family = "binomial",
  standardize = TRUE,
  foldid = NULL,
  topK = NULL,
  thresh = 1e-04,
  nlambda = 100,
  nfolds = 10,
  optim_control = list(maxit = 200),
  ...
)
}
\arguments{
\item{X_pcs}{Matrix of predictor variables.}

\item{y_raw}{Vector of continuous exposure or response variable.}

\item{thresholds}{Vector of thresholds at which to binarize y_raw.}

\item{covariates}{Optional matrix or data frame of covariates to adjust for. Covariates are unpenalized. An intercept is automatically added if not present.}

\item{lambda1}{Ridge penalty weight (defaults to 0.5).}

\item{lambda2}{Fusion penalty weight (defaults to 0.5).}

\item{family}{Model family. Supported values are \code{"binomial"}, \code{"gaussian"}, and \code{"poisson"}. Defaults to \code{"binomial"}.}

\item{standardize}{Boolean indicating whether to scale the predictor matrix. Defaults to TRUE.}

\item{foldid}{Optional fold IDs. Ignored (provided for compatibility with \code{fusedRidge}).}

\item{topK}{Optional integer. If provided, fits a first pass model, selects the union of the \code{topK} largest absolute feature weights across all thresholds, and refits the model using only those features.}

\item{thresh}{Convergence threshold. Ignored (provided for compatibility with \code{fusedRidge}).}

\item{nlambda}{Number of lambda values. Ignored (provided for compatibility with \code{fusedRidge}).}

\item{nfolds}{Number of folds. Ignored (provided for compatibility with \code{fusedRidge}).}

\item{optim_control}{Optional list of control parameters passed to \code{optim}. Defaults to \code{list(maxit = 200)}.}

\item{...}{Additional arguments passed to \code{optim}.}
}
\value{
A list of class \code{c("fusedRidgeDirect", "fusedRidge")} containing:
        \item{fit}{The raw optim fit object.}
        \item{cv}{NULL (placeholder).}
        \item{optimal_lambda}{NA (placeholder).}
        \item{a0}{0 (intercepts are included in \code{coefs_covs}).}
        \item{coefs_covs}{Reconstructed covariate coefficients matrix.}
        \item{coefs_full}{Reconstructed full coefficient matrix.}
        \item{theta_matrix}{Coefficients in the original space.}
        \item{H}{Identity matrix placeholder.}
        \item{thresholds}{Input thresholds vector.}
        \item{family}{Model family.}
        \item{y_matrix}{Binarized target matrix.}
        \item{mean_X}{Scaling center parameters.}
        \item{sd_X}{Scaling scale parameters.}
        \item{covs_names}{Names of covariates.}
}
\description{
Fits a joint Fused Ridge regression model across multiple binarized thresholds of a
continuous response variable using direct optimization (L-BFGS). This avoids the memory-intensive
Kronecker product design matrix expansion used in \code{fusedRidge}, making it highly scalable
for high-dimensional predictors.
}
\details{
Fits a joint Fused Ridge regression model across multiple binarized thresholds of a continuous response variable,
smoothing the coefficient trajectories using a 1D Graph Laplacian. Unlike \code{fusedRidge}, which transforms the
design matrix using a Kronecker product expansion, \code{fusedRidgeDirect} optimizes the loss function directly
using R's built-in \code{optim} function with the \code{"L-BFGS-B"} method and analytical gradients.
}
